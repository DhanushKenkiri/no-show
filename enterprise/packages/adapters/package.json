{
  "name": "@noshow/adapters",
  "version": "0.1.0",
  "description": "Platform adapters for @noshow/core — Luma, and a generic REST adapter for platforms without an API.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": { "@noshow/core": "0.1.0", "viem": "^2.55.19" },
  "devDependencies": { "typescript": "^5" },
  "engines": { "node": ">=20" }
}
